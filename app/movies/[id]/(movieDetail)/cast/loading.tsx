import Spinner from "@/components/common/Spinner";

const LoadingCast = () => {
  return (
    <div className="w-full h-[50vh] flex-center">
      <Spinner />
    </div>
  );
};

export default LoadingCast;
